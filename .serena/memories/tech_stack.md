# Tech Stack

- Documentation-only repository: Markdown and YAML.
- Git is initialized; no package manager, application framework, build system, formatter, linter, or test runner is configured.
- Serena project: version observed at onboarding 1.6.0; JetBrains backend; UTF-8; repo config at `.serena/project.yml`.
- Do not introduce a documentation generator or dependency until the user requests publishing or automation requirements.
